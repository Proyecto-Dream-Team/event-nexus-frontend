import { Outlet } from "react-router-dom"

export const ModuleEvents = () => {
    return (
        <>
                {/* <NavEvents></NavEvents> */}
                <Outlet></Outlet>
        </>
    )
}